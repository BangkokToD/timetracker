# timetracker

Linux time-tracker (GUI + tray) — MVP.

## Requirements
- Python 3.12+

## Install (venv)
```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e ".[dev]"
````

## Run

```bash
python -m app
```

## Run tests

```bash
pytest
```